<!DOCTYPE html>
<html>
   <body>
        <div id="wrapper" class="clearfix">
            <!-- 左カラム　開始 -->
            <div id="leftColumn">
                <hr class=straghtline>
                <h3>撮影予約一覧</h3>
                <hr class=dotline>
                
                
                
                <% @requests.each do |request| %>
                    <p><%= link_to(request.name, "/requests/detail/#{request.id}") %></p>
                    <p><%= request.date %>   　✈<%= request.place %></p>
                    
                    <hr class=dotline>
                <% end %>
            </div>
            <!-- 左カラム　終了 -->
        
            <!-- 右カラム　開始 -->
            <div id="rightColumn">
                右ナビエリア
            </div>
            <!-- 右カラム　終了 -->
        </div>
      
      
      
      
  </body>