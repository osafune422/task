<!DOCTYPE html>
<html>
   <body>
        <div id="wrapper" class="clearfix">
            <!-- 左カラム　開始 -->
            <div id="leftColumn">
                <hr class=straghtline>
                <h3>予約詳細</h3>
                <hr class=dotline>
                
                <h4><%= @request.date %></h4>
                <p><%= @request.name %></p>
                <p><%= @request.genre %>   　✈<%= @request.place %></p>
                
                <% if @request.pic1 != nil %>
                    <% 5.times do |num| %>
                        <img src="<%= "/pictures/#{@request.date.year}/#{@request.date.month}/#{@request.date.day}/#{@request.date}_#{num+1}.jpg" %>" width=20%>
                    <% end %>
                <% end %>
                <br>
                
                <%= link_to("撮影データ送信", "/requests/upload/#{@request.id}") %>
                
                <br>
                
            </div>
            <!-- 左カラム　終了 -->
        
            <!-- 右カラム　開始 -->
            <div id="rightColumn">
                右ナビエリア
            </div>
            <!-- 右カラム　終了 -->
        </div>
      
      
      
      
  </body>